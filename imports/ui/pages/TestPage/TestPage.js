import React from 'react';

// import useSnackbars from '../../../api/notifications/snackbarConsumer';

export default function TestPage() {
  // const { addAlert } = useSnackbars();

  return (
    <div>
      {/* <button onClick={() => addAlert({ message: 'SNACK', severity: 'error' })}>
        NOTIFY
      </button> */}
    </div>
  );
}
